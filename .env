# 数据库连接串。换库只改这一行。
# 格式: postgresql+psycopg2://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql+psycopg2://postgres:144714@localhost:5432/python_learning

# 想切回 SQLite 时,注释上面那行、打开下面这行即可(验证"一处切换"):
# DATABASE_URL=sqlite:///database/app.db
